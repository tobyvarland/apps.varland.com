ruby:
  provide(:title, 'Open Purchase Orders')

- content_for :nav do
  = render "nav"

.d-flex.flex-row.align-items-center.justify-content-between.mt-3
  h1.font-varland Open Purchase Orders

= render partial: "po_vendor", collection: @data[:vendors], as: :vendor