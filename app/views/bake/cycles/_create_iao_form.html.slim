= simple_form_for(cycle, url: :bake_cycles, html: {class: ["vp-gray-form", "my-3"]}) do |f|

  = f.input :type, as: :hidden

  .row
    = f.simple_fields_for :stands do |form|
      .col-12.col-md.mt-3
        = form.input :type, as: :hidden
        = form.input :name, label: "#{form.object.type == "Bake::LargeStand" ? "IAO Stand" : "Rod Cart"} #:", input_html: { autofocus: true }

  .form-actions
    = f.button :button, "#{fa "check"} Create #{iao_label} Cycle".html_safe, class: ["btn-primary", "btn-sm"]